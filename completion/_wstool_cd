#compdef wstool_cd

_wstool_cd () {
  local ws_root
  # check if in workspace
  ws_root=$(_wstool_cd_get_workspace 2>/dev/null)
  [ "$ws_root" = "" ] && return 1

  local -a _repos
  _repos=( $(wstool info -t $ws_root --only=localname) )
  _describe -t repos "Repositories" _repos
}
