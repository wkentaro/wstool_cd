#compdef wstool_cd

_wstool_cd () {
    # check if in workspace
    wstool info --managed-only >/dev/null 2>&1 || return 1

    local -a _repos
    _repos=( $(wstool info --only=localname) )
    _describe -t repos "Repositories" _repos
}